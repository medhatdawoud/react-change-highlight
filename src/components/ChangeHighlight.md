Highlighting the changed part of children

```jsx
import ChangeHighlight from './ChangeHighlight';

<ChangeHighlight>
  <div ref={React.createRef()}>This is the content of the children loaded inside the ChangeHighlight</div>
</ChangeHighlight>
```